import { StackedBarChartDirective } from './stacked-bar-chart.directive';

describe('StackedBarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new StackedBarChartDirective();
    expect(directive).toBeTruthy();
  });
});
